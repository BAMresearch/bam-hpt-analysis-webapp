The import notebook writes one Plotdaten Excel file per selected test sheet here.

The WebApp reads this folder (`flask_app/config.json` → `data_dir`, default `../Plotdaten` when started from `flask_app`).

Excel files are not committed to GitHub. Only this README is.
